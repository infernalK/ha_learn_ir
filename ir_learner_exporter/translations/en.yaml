configuration:
  export_filename:
    name: Export filename
    description: Name of the generated JSON file.
  default_manufacturer:
    name: Default manufacturer
    description: Pre-filled manufacturer name in the UI.
  default_supported_controller:
    name: Supported controller
    description: Controller type stored in the exported JSON.

network:
  8099/TCP: Internal web UI port used by Ingress.